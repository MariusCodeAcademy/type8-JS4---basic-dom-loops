console.log('main.js');

// parasom for cikla kuris paima skaicius nuo 0 iki 4 imtinai
// sudedam ir gaunam bendra suma
let total = 0;
// debugger;
for (let i = 0; i <= 4; i++) {
  // let total = 0;
  // console.log(`cycle number ${i + 1} total: ${total} + i: ${i}`);
  total = total + i;
  // console.log(` === ${total}`);
}
console.log('total ===', total);
